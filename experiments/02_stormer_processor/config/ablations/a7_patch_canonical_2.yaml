# A7 ablation — canonical patch size p=2 (CLAUDE.md §6, ablation A7).
# Pair with _base_ablation.yaml.
# Smaller canonical size → more tokens per patch budget → finer spatial resolution.
# Ablates the canonical p sweep (2, 4, 8).
model:
  init_args:
    patch_size: 2
data:
  init_args:
    canonical: 2
